name: Releases

on:
  push:
    tags:
      - v*
    branches:
      - main

jobs:
  release:
    uses: pimalaya/nix/.github/workflows/releases.yml@master
    secrets: inherit
    with:
      project: himalaya
      nixpkgs: staging-next
